# DESIGN.md — ひらがじゃん「和紙と墨 / Washi & Sumi」

> design.md（google-labs-code）の方針に倣い、**トークン（厳密値）** と **プロセ（なぜ）** を併記する。
> 数値だけを真似ると“AIあるある”の汎用デザインに戻る。値は必ず物語から導く。

## ナラティブ（物語）

**文机（ふづくえ）の上の一局。** ひらがじゃんは、象牙色の牌でひらがなの単語を組む「言葉の craft」。
だから舞台は SaaS のダッシュボードではなく、**和紙を広げた文机**であるべきだ。
背景は生成りの和紙、文字は墨。色は足さない——ただ一点、確定の瞬間にだけ捺す**朱の印（はんこ）**を主役に据える。
旧デザイン（Linear 風ダーク＋藍紫グロー）は牌のアイボリーと喧嘩し、どこにでもある“ダークSaaS”だった。
ここでは色数を絞ることで、逆に固有性を立ち上げる（Constraint breeds originality）。

## カラートークンと、その物語

| 役割 | トークン | 値 | 物語／使いどころ |
|---|---|---|---|
| 文机（地） | `--bg` | `#ece3d0` | 深めの生成り和紙。最背面。 |
| 和紙（紙） | `--panel` | `#faf5ea` | 机に置いた一枚。カード・モーダル・操作盤＝最も明るい＝最前面。 |
| 凹（沈み） | `--surface`〜`--surface3` | `#f2ead9`→`#dfd3b8` | 入力欄・牌山・チップ等の「沈んだ面」。明→暗で沈み込みを表す。 |
| **朱（印）** | `--accent` | `#c8442e` | **唯一の主役**。確定・作成・宣言・進行中の単語。捺すように使い、多用しない。 |
| 竹（緑） | `--jade` | `#4a7a3f` | 成長＝あがり・自分の番・ツモ・ポン（鳴き）。 |
| 金 | `--gold`/`--amber` | `#b8902f`/`#9c7416` | 得点・ドラ・賞。`--amber` は明地で読める深金。 |
| 緋（煉瓦） | `--crimson` | `#a4231b` | **エラー専用**。朱と役割を分けるため、より暗い煉瓦色で隔てる。 |
| 墨 | `--text`〜`--text-3` | `#211c15`→`#a99d87` | 本文〜極小ラベル。墨の濃淡で階層を作る。 |

縁（`--border*`）は「白の半透明」ではなく **墨の低アルファ**（明地では白縁は消える）。
影（`--shadow*`・`--tile-shadow`）は黒ではなく **暖かい墨色を低アルファで薄く**（明テーマは影を弱く）。

## 牌（タイル）

- **面（自分の手牌）= 骨象牙**：和紙よりさらに白い `--tile-face` 系。地に対して「物」として浮くよう、明るい面＋くっきりした縁＋柔らかい影で立体に。
- **背（相手の伏せ牌）= 竹緑**：本物の麻雀牌の裏に倣い緑にする。面=象牙／背=緑の対比そのものが固有の手触り。
- **鳴き牌 = 金**、**待ち牌（立直）= 朱の血色**、**ドラ = 朱の差し色**。

## タイポグラフィ（目的でペアリング）

- 見出し／物語（ロゴ・モーダル見出し・手番バナー）= **Shippori Mincho（明朝）** `--display`。明朝の縦画が「書」の品位を与える。
- 本文・UI = **Zen Kaku Gothic New** `--font`。可読性の高い角ゴシック。
- 数字（部屋ID・得点）= **JetBrains Mono** `--mono`。券面・印影のような無機質さ。

## モーション／エレベーション

- `--ease*`（標準/出/スプリング）と `--shadow1..3` は既存を踏襲（暗→明で影アルファのみ低減）。
- 演出CSSは必ず `@media (prefers-reduced-motion: reduce)` で停止分岐を持つ（confetti / tile-pop / turn-shine）。
- 「印を捺す」瞬間＝確定・あがりは朱／金で強く、平時は墨と和紙で静かに。**強弱のコントラストが主役**。

## 適用ルール（破ってはいけない）

1. 色・余白をハードコードしない。必ず `:root` のトークンを使う。
2. **朱は主役。連発しない。** 確定・作成・進行中の単語など「いま重要な一点」だけに。
3. 新規UIの背景は `--panel`（前面）か `--surface*`（沈み）から選ぶ。地（`--bg`）に直接置かない。
4. エラーは `--crimson`、成功・進行は `--jade`、得点・賞は `--gold/--amber`。朱（accent）で代用しない。
5. デザイン改修をデプロイするたび `public/*.html` の `?v=` を揃えて上げる（`test/assets.test.js` が検査）。
